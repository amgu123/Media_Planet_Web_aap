spring:
  application:
    name: media-planet-backend
  
  datasource:
    url: jdbc:mysql://srv1275.hstgr.io:3306/u370330862_mediaplanet?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
    username: u370330862_mediaplanet
    password: 'ZuADFy10:'
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      max-lifetime: 60000       # 1 minute
      keepalive-time: 25000     # 25 seconds
      connection-timeout: 30000
      idle-timeout: 30000       # 30 seconds
      validation-timeout: 5000
      connection-test-query: SELECT 1
      minimum-idle: 1
      maximum-pool-size: 10
  
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: false
  
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

server:
  port: 9090
  servlet:
    context-path: /api

# JWT Configuration
jwt:
  secret: 404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
  expiration: 86400000 # 24 hours in milliseconds

# CORS Configuration
cors:
  allowed-origins: http://localhost:4200
  allowed-methods: GET,POST,PUT,DELETE,OPTIONS
  allowed-headers: "*"
  allow-credentials: true

logging:
  level:
    root: INFO
    com.mediaplanet: DEBUG
    org.springframework.security: DEBUG
    com.zaxxer.hikari: INFO
